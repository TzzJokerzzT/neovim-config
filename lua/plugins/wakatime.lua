return {
  {
    "wakatime/vim-wakatime",
    lazy = false,
    -- config = function()
    --         -- Especifica la ruta al CLI de Wakatime (instalado con pipx)
    --   vim.g.wakatime_CLIPath = vim.fn.expand("~/.local/bin/wakatime")
    --   -- Opcional: Configuración adicional (si es necesaria)
    --   vim.g.wakatime_ScreenRedraw = 0 -- Desactiva redibujado de pantalla
    -- end,
  },
}
